# build artifacts
*.o
*.a
*.so
/build/
/build-*/
/build-test-*
# per-verb effector binaries + spike/diagnostic tools
/runix-apt-*
/pkgexec-probe
/pkgexec-plan
# fuzzer (the seed corpus in fuzz/corpus/*.json is tracked)
/fuzz-request
crash-*
leak-*
timeout-*
oom-*
fuzz/corpus/*
!fuzz/corpus/*.json
# packaging (build outputs; debian/ sources stay tracked)
*.deb
*.buildinfo
*.changes
/debian/pkgexec/
/debian/.debhelper/
/debian/debhelper-build-stamp
/debian/files
/debian/*.substvars
/debian/*.debhelper.log
/debian/tmp/
